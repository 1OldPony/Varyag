@model IEnumerable<Varyag.Models.Foto>


<table>
    <tr>
        <td>
            <a asp-action="Create" asp-controller="Fotoes">Добавить фотку</a>
        </td>
    </tr>
</table>
<br />
<br />
@foreach (var item in Model)
{
<table float="left">
    <tr>
        <td rowspan="3">
            <img alt="@item.Alt" title="@item.Name" width="120" height="120" src="@Url.Action("ImageRender", "Fotoes", new { id = item.FotoID})">
        </td>
        <td>
            @Html.DisplayNameFor(model => model.Alt)
        </td>
        <td>
            @Html.DisplayNameFor(model => model.Name)
        </td>
    </tr>
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Alt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
    </tr>
    <tr>
        <td colspan="2">
            <a asp-action="Delete" asp-controller="Fotoes" asp-route-id="@item.FotoID">Удалить</a>
        </td>
    </tr>
</table>
}
<br />

    @*<table class="table" float="left">
            <thead>
                <tr><td colspan="6"><a asp-action="Create" asp-controller="Fotoes">Добавить фотку</a></td></tr>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Alt)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ProjectFoto)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ShipProject)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.News)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Alt)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            <img alt="@item.Alt" title="@item.Name" width="80" height="80" src="@Url.Action("ImageRender", "Fotoes", new { id = item.FotoID})">
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ShipProject.ProjectID)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.News.NewsId)
                        </td>
                        <td>
                            <a asp-action="Delete" asp-controller="Fotoes" asp-route-id="@item.FotoID">Удалить</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>*@
